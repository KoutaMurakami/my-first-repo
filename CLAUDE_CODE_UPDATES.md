# Claude Code 更新履歴

Last recorded version: 2.1.248

このファイルは Claude Code (Anthropic の CLI コーディングエージェント) の
リリース内容を定期的に追跡し、日本語で要約したものです。新しい日付のセクションほど
ファイルの上に追加されます。

## 2026-08-28

### 2.1.248
- 組み込みのコマンド/コードツールとWebFetchを外し、ファイルツールも作業ディレクトリ内に限定する`--restricted`フラグを追加
- エージェントのfrontmatterでプロンプトキャッシュTTLをエージェント単位に設定できる`experimental.cacheTtl`を追加
- `claude self-hosted-runner --client-label`でランナー登録名(デフォルトはホスト名)を上書きできるよう追加
- サーバー管理設定の読み込み失敗を起動時警告や`/doctor`/statusで説明する診断機能を追加
- 大規模リポジトリへのプッシュ時にGitHub CLIトークンに`workflow`スコープが無い場合の警告を`/web-setup`に追加
- AWS Marketplace請求のEnterprise組織向けに上限引き上げを申請できる`/usage-credits`コマンドを追加
- Bedrock/Vertex/Foundry利用時やテレメトリ無効時でもセッション間メッセージング(SendMessage/ListAgents)を利用可能に
- OAuthリフレッシュ後にツール定義が再描画され、長時間セッションで約1時間ごとにプロンプトキャッシュミスが発生する不具合を修正
- ScheduleWakeupツール定義の変更により、使用量超過に入った再開セッションで全プロンプトキャッシュミスが起きる不具合を修正
- Claude DesktopおよびCowworkセッションが30日後に消える不具合を修正し、デスクトップ発のセッションを保持するよう改善
- トークン更新ロックを別プロセスが保持している間にログイン画面へリダイレクトされる不具合を修正
- Windows: セッションからデタッチ後やwin32-input-mode起動後に`claude agents`一覧のキー操作が効かない不具合を修正
- ANTHROPIC_API_KEY設定済み環境でのConsoleサインイン推奨がURL表示前にOAuthエラーで失敗する不具合を修正
- `/model`やfast-mode通知でモデル名に`[1m]`等のサフィックスがリンクにならず素のまま表示される不具合を修正
- CI環境変数設定時に`claude agents`がワークスペース信頼確認をスキップしてしまう不具合を修正
- PRステータスキャッシュが壊れている場合に`claude agents`が起動時にクラッシュする不具合を修正
- マシン再起動後にエージェント表示が数週間前のバックグラウンドセッションを復活させてしまう不具合を修正し、正しく停止状態を表示するよう改善
- エージェント表示で古い会話を開くと入力中のプロンプトが消える不具合を修正
- 別ターミナルで再開した停止セッションを`claude agents`から開くと重複プロセスが生成される不具合を修正
- ワークツリーのブランチがマージ済みだが未プッシュの場合に`claude agents`と`claude rm`が削除を拒否する不具合を修正
- PermissionRequest/PreToolUseフックが不正な応答をした際にバックグラウンドセッションが無言で待機する不具合を修正し、フック名とエラーを表示するよう改善
- フックの標準出力が有効なJSONでない場合、プレーンテキストとして扱われてしまう不具合を修正しフックエラーとして報告するよう改善
- プロジェクトの`.mcp.json`のclaude.aiコネクタが`/mcp`一覧で誤った見出しに表示される不具合を修正
- `headersHelper`を使うMCPサーバーが401時にOAuthディスカバリに入ってしまう不具合を修正し、ドキュメント通りヘルパーで再試行するよう修正
- 管理設定のセキュリティ承認ダイアログが必要な場合にClaudeアプリゲートウェイへの`/login`が固まる不具合を修正
- apiKeyHelperのみが認証情報の場合にゲートウェイのモデル検出が実行されない不具合を修正
- `claude logs`終了後にターミナルのマウストラッキングと代替スクリーンモードが有効なままになる不具合を修正
- 長いルールが絵文字の途中で切れると信頼ダイアログに文字化けが表示される不具合を修正
- Ctrl+C後のShift+Tabで権限モード表示が「Press Ctrl-C again to exit」ヒントの裏に隠れる不具合を修正
- `/ultrareview`やクラウドセッションの種付けで、未コミットの認証情報ファイルやエディタのスワップファイルがアップロードされてしまう不具合を修正
- サイレント再接続後にRemote Controlセッションが権限確認や最新メッセージを表示しないことがある不具合を修正
- セッション認証情報が読み込み可能になる前にクラウドセッションが起動失敗する不具合を修正
- サブコマンドの前にグローバルフラグを置くと`claude remote-control`が自分自身のフラグを拒否する不具合を修正
- 起動時警告がトランスクリプトから1列ずれて表示される不具合を修正
- バックグラウンド化したワークツリーセッションでチェックアウトが失われる不具合を修正し、実行中はロックを保持するよう改善
- 非ラテン文字(韓国語IME入力など)で入力した名前が@メンションにマッチしない不具合を修正
- 不正な`crossSessionInbound`値が無言で無視される不具合を修正し、警告またはメッセージ拒否をするよう改善
- 組織で`/usage-credits`が使えない場合に、レート制限・使用量メッセージがこのコマンド実行を案内してしまう不具合を修正
- VSCode: セッションが未保存のまま「No conversation found」で固まるチャットタブを修正し、新しい会話を開始するよう改善
- Workflowツールのプロンプト使用量を約5.7kトークンから約1kトークンに削減し、スクリプト作成リファレンスを同梱スキルに移動
- PRバッジがPR未変更時はGitHubへの問い合わせ頻度を下げるよう改善(プッシュや`gh pr`実行時は即時更新)
- 管理設定でのクライアント側タイムアウト・MCP起動モード・ストリームウォッチドッグ環境変数が承認確認を要求しないよう改善
- `/ultrareview <PR#>`起動前にGitHubアカウントのアクセス権を確認し、問題時の対処法を案内するよう改善
- 既定の一時ディレクトリが使えない場合、セッション間メッセージングが非公開の`/tmp`ディレクトリにフォールバックし通知するよう改善
- エージェント表示のディスパッチ入力でShift+Enterが改行、Ctrl+Enterで送信・アタッチするよう変更
- `/loop`のダイナミックモードとプロンプト無し自律モードのデフォルトをBedrock/Vertex/Foundryでも常時利用可能に変更
- Anthropicテレメトリのエクスポート失敗ログを、OTEL診断エラーではなく`[Anthropic telemetry]`のデバッグレベルログに変更
- Linuxユーザー名前空間でのセッション間メッセージングにおいて、root相当の信頼を正規のシステムディレクトリに限定するよう変更
- SendMessageの結果表示を、返信は親セッションの会話に届く(サブエージェントには届かない)ことが分かるよう変更

## 2026-08-27

### 2.1.247
- セッション内で問題が起きた際にフィードバック報告を下書きし`/feedback`から送信確認できる`SendFeedback`ツールを追加
- 組織独自のヒントを組み込みのものと交互表示できるよう`spinnerTipsOverride`に項目やファイル指定を追加
- Bashの権限確認にAutoモードへの一発切り替えオプション付きヒントを追加
- 既存プロジェクトのClaude API利用料を分析しコスト削減策を段階的に進める`/claude-api cost-optimize`を追加
- `/claude-api`スキルにAdmin API(組織メンバー・招待・ワークスペース・APIキー等)の解説を追加
- 履歴検索や`/config`等ですばやい矢印キー+Enter操作が一つ上の行に効いてしまう不具合を修正
- サブエージェントがモデル404で落ちる際にフォールバックモデル連鎖を使うよう修正し、エラー情報を親に詳しく返すよう改善
- フックやバックグラウンドエージェントの大量エラー出力で会話が溢れセッションが固まる不具合を修正
- 非ラテン文字キーボード配列でCtrlショートカットが効かない不具合を修正
- マウスレポートが分割受信された際に文字化けが入力される不具合を修正
- Bashサンドボックスの後処理がdotfile管理のシンボリックリンク設定ファイルを削除してしまう不具合を修正
- `/terminal-setup`がZedのkeymap.jsonを上書きしてしまう不具合を修正しマージ方式に変更
- `/rename`失敗時にセッション登録が更新できなくても成功したように見える不具合を修正
- `--agent`起動セッションでの`/compact`が本来のシステムプロンプトを使わない不具合を修正
- ホストプロセス終了後に「opening…」表示のまま固まるバックグラウンドセッション表示を修正
- フックやバックグラウンドタスクの出力書き込み失敗時のメモリ増加を修正
- `/install-github-app`のSSH利用時の表示を改善
- フォアグラウンドから引き継いだシェルコマンドの終了表示不具合を修正
- バージョンなしマーケットプレイスプラグインの再インストール時にキャッシュが壊れる可能性を修正
- Remote Controlセッションが作業ツリーの差分を報告しない不具合を修正
- セルフホストランナーセッションの起動通知が早すぎる不具合を修正
- 管理設定でのゲートウェイサインイン利用時に初回セットアップが接続エラーで失敗する不具合を修正
- モード切り替え直後にクラウドセッションが古い権限モードを表示する不具合を修正
- コンテナ再起動をまたぐクラウドセッションが無言になる不具合を修正し再開時に状況を報告するよう改善
- Sonnet 5のデフォルト自動圧縮を1Mコンテキスト全体を使うよう変更
- セッション間ピアメッセージをデフォルトで1行プレビュー表示に変更(Ctrl+Oで展開)
- 不審なリンク先(ネットワークパスや制御文字を含むもの)をプレーンテキスト表示するよう変更
- プロンプトフッターのPRバッジの再チェック頻度を調整
- 管理設定によるゲートウェイサインイン強制時の分析機能を常時無効化するよう変更
- Claude Code用ゲートウェイサインインリクエストにCode識別情報を追加
- 組織のサインイン強制について管理設定が読めない場合は起動時に終了するよう変更

## 2026-08-26

### 2.1.246
- Bashのワイルドカードルールがサブコマンドより前のオプションにマッチする場合の起動時警告を追加
- `/permissions`にAutoモードの分類ルールを確認できる「Auto mode」タブを追加
- ターン所要時間の表示にターン完了時刻を追加
- フルスクリーンモードのリサイズ後の表示崩れ・長い行での大幅な遅延・スクロール固着を修正
- 特定条件でのバックグラウンドセッション起動失敗(45秒待ち、npm再インストール中のEACCES)を修正
- 先頭にマークアップがない場合にMarkdownレンダリングが無効化される不具合を修正
- MCPツール呼び出しの中断誤報告と引数がJSON文字列で送られる不具合を修正
- ←キーや`/background`で完了済みサブエージェントが再起動してしまう不具合を修正
- `claude agents`のバックグラウンドセッション二重表示、保持クリーンアップによるgit worktree誤削除を修正
- 大規模セッションでAutoモードのツール呼び出しが誤って拒否される不具合を修正
- プラグイン関連の複数の不具合(キャッシュ重複、BOM付きplugin.jsonでのインストール失敗、`claude plugin update`失敗、`/reload-plugins`のカウント誤り)を修正
- カスタムテーマの枠色・差分色が無視される不具合、未知アクション付きキーバインドが無反応になる不具合を修正
- UTC+タイムゾーンでの`/stats`ヒートマップのずれ、`/fork`後の会話が空になる不具合を修正
- パス補完のnullバイト起因の失敗、ステータスラインのコスト/所要時間リセットを修正
- 大きなファイル上書き後にWriteツールが「Out of memory」と誤報告する不具合を修正
- 未対応のツールブロックを含む履歴からの再開が400エラーになる不具合を修正
- Team/Enterpriseでの`curl`インストール失敗、VS Code拡張でplanモード終了セッションの再開挙動を修正
- サンドボックス権限確認中にNotificationフックが発火しない不具合を修正
- テレメトリリクエストにサードパーティゲートウェイのAPIキーが混入する不具合を修正
- 短命JWTでのアイドル復帰時APIエラー、長時間セッションでのメモリ増加を修正
- `/ultrareview`やクラウドセッションが未コミット変更ありの状態で始まる不具合を修正
- セルフホストランナーがwork-poll応答不正時にセッションを終了してしまう不具合を修正
- `/cd`がプロジェクト設定・フック・MCPサーバー・スキルを即座に反映するよう改善
- スナップショット関数再生によるBashツールのレイテンシ改善
- `/code-review`をBedrock/Vertex/Foundryでも開始可能に変更

## 2026-08-25

### 2.1.245
- glibc 2.44を採用するLinuxディストリビューションで起動時にクラッシュする不具合を修正

### 2.1.243
- `/usage`にLoopsの内訳(実行回数・合計トークン・実行あたりトークン・最終実行)を追加し、暴走/多発する`/loop`タスクを把握しやすく
- `/model`ピッカーをカスタマイズできる`modelPicker`設定を追加(Vertex/Bedrock ID含む任意のモデルを順序付き・ラベル付きで追加/置換可能)
- `promptCacheTtl`/`subagentPromptCacheTtl`設定を追加し、APIキー/クラウドプロバイダー利用時にメイン会話は1時間、サブエージェントは5分のプロンプトキャッシュを維持可能に
- 組織の契約料金・割引率を`/cost`やステータスライン、テレメトリのコスト表示に反映する`modelPricing`マネージド設定を追加
- `/login`にAnthropic Consoleアカウントでのキーレスサインインを追加(APIキーを許可しない組織向け)
- `/status`に、優先度の高いマネージド設定により適用されなかった設定ソースを示す「Skipped sources」行を追加
- `/mcp`/`/plugins`で組織が認証を管理するclaude.aiコネクタに「managed」マークを追加
- GitHub未接続のclaude.aiユーザーへ`/web-setup`を案内するヒントを追加
- `/status`にClaude Code on the web向けGitHub接続状況の表示を追加(Pro/Max、未接続時は`/web-setup`を案内)
- `/tasks`とエージェント詳細ダイアログに、各サブエージェントが使用したモデルとeffortレベルを表示するように
- 非対話(`-p`)/SDKセッションでリモートMCPサーバーが切断後に復旧できない不具合を修正(自動再接続または失敗報告するように)
- デスクトップアプリからのMCPサーバーサインインが「Invalid redirect URI」で失敗する不具合を修正(Linear等)
- サーバー側の一時的な無効化がキャッシュされ、その後のフラグ取得も失敗するとautoモードが起動時から使えなくなる不具合を修正
- API過負荷時の再試行要求で約1分待った後、autoモードのツール呼び出しが「一時的に利用不可」として拒否される不具合を修正
- `/model`ピッカーでUltracode選択が無視される不具合を修正
- `/resume`が直近50件のセッションしか表示しない不具合を修正(スクロールで追加読み込み)
- クラウドセッションがターン中の再起動後に再開する際、保留中のフック/バックグラウンドタスク通知が通常の継続メッセージでなくプロンプトとして再送されてしまう不具合を修正
- 2.1.232のソケットディレクトリ強化後、ユーザー名前空間やrootlessコンテナでクロスセッションメッセージングが無言で無効化される不具合を修正
- 画面の別部分が再描画されるとコンテナ外にはみ出すテキスト(`/login`のサインインURLなど)の先頭列が失われる不具合を修正
- 絵文字の直後に入力した誤字に`spellcheck`が下線を引かない不具合を修正
- 直前のバックグラウンドBashタスク完了時にバックグラウンドサブエージェントが起こされない不具合を修正
- Anthropic APIが応答を開始しないままセッションが10分以上無反応になる不具合を修正(約3分でタイムアウトし1回再試行後エラー表示)
- 認証・モデル利用可否などクライアント生成のエラーメッセージがモデル出力のように表示される不具合を修正
- CIでのワークロードアイデンティティ連携を修正(1ジョブ内のプロセスが使い捨てトークンを再交換せず共有、交換失敗時は即座にエラー)
- `/logout`後の初回起動などサインインで始まるセッションで、サーバー管理の`companyAnnouncements`が起動時に表示されない不具合を修正
- `$()`やバッククォートのコマンド置換後に引数が続くと、`Bash(cat *)`のようなフックの`if`条件が無関係なBashコマンドに反応してしまう不具合を修正
- `marketplace`フィールド付きで宣言されたプラグイン依存関係が、`--plugin-dir`で両方読み込んでも解決されない不具合を修正
- 最後のLSPプラグイン無効化後も`/reload-plugins`がLSPツールを保持してしまう不具合を修正(会話再読み込みを伴うLSP変更前に警告するように)
- `--agents`が不正なJSON/エージェント定義を無言で無視する不具合を修正(`--mcp-config`同様に明確なエラーで終了)
- `~/.claude.json`のMCPサーバー設定が不正な場合、`/status`がファイル名なしで「Found invalid entries in: .」と表示する不具合を修正
- `/clear`後もセッション名は引き継がれるのに、プロンプトバーから`/rename`名が消える不具合を修正
- `~/.claude/history.jsonl`に不正な行があるとCtrl+R履歴検索や上矢印履歴が壊れる不具合を修正
- modifyOtherKeys/kittyプロトコルを使う端末でCtrl+[がvimのINSERTモードを抜けられない不具合を修正
- `no_proxy`(小文字)ではなく`NO_PROXY`に`localhost`を指定した場合、ローカルIDE接続が`HTTPS_PROXY`経由になり失敗することがある不具合を修正(両方の大文字小文字に対応)
- ブロックされたコマンドが終了コード0を返す場合(プロキシの403ページを出力する`curl`など)、サンドボックスのネットワーク違反詳細がBashツール結果から欠落する不具合を修正
- レート制限ウィンドウのリセット後もセッションがアイドルだとステータスラインの`rate_limits`や`/usage`がリセット前の使用率を表示し続ける不具合を修正
- 未コミットの変更があると`claude --teleport <session>`が終了してしまう不具合を修正(セッションピッカー同様、stashして続行を提案するように)
- 古いGitHub CLI(`gh auth token`非対応)で既に認証済みでも`/web-setup`が繰り返しログインを求める不具合を修正
- 自動更新でセットアップ時のバージョンがクリーンアップされるとClaude in ChromeがClaude Codeとの接続を失う不具合を修正(安定版`claude`ランチャー経由で起動するように)
- [VSCode] 機能フラグ取得前(インストール直後など)に開始したセッションがautoモードや設定した既定モードでなくデフォルト権限モードで開いてしまう不具合を修正
- [VSCode] サブエージェントのツール活動中にFocusビューで展開したセクションが勝手に折りたたまれる不具合を修正
- 起動時間を改善(サンドボックス/MCP起動が初期描画をブロックしなくなり、素の起動でサブコマンド登録を省略、ワークフロー検出・設定・信頼ストアの処理を軽量化)
- ネイティブインストール・自動更新のダウンロードサイズを改善(zstd圧縮によりLinux x64で約340MBから約75MBに)
- `ANTHROPIC_AUTH_TOKEN`でAnthropic APIに直接認証するセッションの利用テレメトリを組織に正しく帰属させるよう改善(データ取り扱い設定が適用されるように)
- ネイティブバイナリサイズを約2MB削減(同梱スキル・プロンプトテキストをより効率的に格納)
- ネイティブビルドのメモリ使用量を改善(バンドル全体を常駐させず必要時読み込みに、セッションあたり約40〜70MB削減)
- 長時間セッションでのピークメモリ使用量を改善(ヒープ増加時に早めにガベージコレクションするように)
- SSH経由の`/login`を改善(サインインURLが即座に表示、`c`キーでコピー方法を正しく報告、フルスクリーンでのテキスト選択方法のヒントを追加)
- effort `xhigh`/`max`を思考モード無効時に使った際のエラーメッセージを改善(該当レベル・無効化した設定・修正方法`/effort high`を明示)
- `/loop`を改善(Claudeが何もすることがない連続ウェイクアップを1行にまとめて表示)
- サンドボックス化したBashツールのプロンプトから許可済みネットワークホスト一覧の表示をやめ、未許可ホストへも(承認を得て)リクエストを試みるように変更
- `/model`ピッカーと同梱の`claude-api`スキルで、Sonnet 5の$2/$10 per Mtok料金を期間限定プロモではなく通常価格として表示するように更新
- macOSのcomputer useで、デスクトップ・Dock・Finderウィンドウのクリックにも他アプリ同様Finderへのアクセス許可が必要になるよう変更
- Bedrock/Vertex/Foundryおよびテレメトリ無効時、`/model`・`/fast`・`/effort`がターン終了を待たず即座に実行されるように変更
- サーバーが環境をセッション中に切断した際`claude remote-control`が終了しRemote Controlセッションが取り残される不具合を修正(復旧できるように)
- 管理者/オーナー権限を持たないTeam/Enterpriseメンバーで、`claude remote-control`停止・再起動後にRemote Controlセッションが固まることがある不具合を修正
- クロスセッションメッセージング用受信ソケットが30秒以内に完全な行を受信しない接続を閉じるよう変更(データ準備後に接続するスクリプト向け)
- 別端末がRemote Controlを保持する会話を再開した際の通知を改善(他マシンのセッションは見えず、到達もできない旨を明示)
- [VSCode] 長時間セッションの履歴トリミングを改善(古いツール活動行から削除し、メッセージと返信を優先的に表示)
- [VSCode] Claudeアカウントでサインイン時、拡張機能自身の利用テレメトリを組織に正しく帰属させるよう改善

## 2026-08-24

### 2.1.241
- 不具合修正と安定性向上(詳細な個別項目は非公開)

## 2026-08-23

### 2.1.240
- 不具合修正と安定性向上(詳細な個別項目は非公開)

## 2026-08-22

### 2.1.239
- データレジデンシー(米国限定推論)ワークスペースのコスト表示に1.1倍の割増料金を反映
- Bedrock/Vertex/Foundryなど従来除外されていた環境でもフルスクリーン表示の初回案内に対応
- Python版anthropicライブラリを0.x→1.xへ移行する`/claude-api upgrade`コマンドを追加
- クラウドセッションでclaude.ai同期プラグインが`name@synced`として表示され、個別に有効/無効化できるように
- Alpine/musl環境でネイティブの画像貼り付け・クリップボード・音声キャプチャ機能が動作するよう修正
- 月間利用上限到達時のメッセージに、セッション/週次上限のリセット時期も表示するよう改善
- Content-Typeヘッダーを除去するプロキシ経由のBedrockストリーミングでAPI呼び出しが二重課金される不具合を修正
- BedrockでSSOプロファイル+awsAuthRefresh使用時、HTTPSプロキシ経由だと起動時にハングする不具合を修正
- 存在しないディレクトリから起動した際にクラッシュダンプが出る不具合を修正し、明確なメッセージを表示するように
- JetBrains IDEのターミナルでEdit/Write実行時に約5秒停止する不具合を修正
- Escキーでプロンプトをキューイングした際にターンが早期終了しセッションが停止したままになる競合状態を修正
- WebFetchが期限切れページを15分を超えてメモリに保持し続ける不具合を修正
- クラウドセッション(Web/デスクトップ/モバイル)がアイドル再起動後にplanモードから復帰してしまう不具合を修正
- ターミナルより高いMCPエリシテーションフォームがフルスクリーンモードで見切れる不具合を修正
- リモートMCPサーバーが一時的な5xxエラー後に再接続できず失敗状態のままになる不具合を修正
- リネーム後の会話が64KBを超えると`/resume`からカスタムセッションタイトルが消える不具合を修正
- `claude -c`/resumeが記号違いだけの別ディレクトリのセッションを誤って選んでしまう不具合を修正
- `/resume`とエージェント一覧が、ファイルが触れられただけのセッションを「最近変更」と誤表示する不具合を修正
- 削除済みディレクトリ(worktree等)のセッションを`/resume`する際、cd指示ではなく現在のディレクトリで再開するように修正
- dark-ansiテーマでフルスクリーン時のツール結果表示が背景と同色になり読めなくなる不具合を修正
- フルスクリーン表示の案内が繰り返し出る問題を修正し、3回表示したら停止するように
- `.worktreeinclude`の`**/`始まりパターンがgitignore対象ディレクトリで機能しない不具合を修正
- UTF-8 BOM付きの.mdファイルで定義したagent/skill/commandが無視される不具合を修正
- `/insights`が一部モデルで`<message>`タグをそのまま出力してしまう不具合を修正
- マーケットプレイスの`metadata.pluginRoot`設定が効かない不具合を修正
- ブラウザ内蔵ターミナルでマウス操作がプロンプトに文字列として混入する不具合を修正
- カスタムテーマでのeffort/ultracodeバッジ色指定が無視される不具合を修正
- PreToolUseフックで遅延実行されたツールがOTelトレースを分断してしまう不具合を修正
- vimモードでEscapeを押すとNORMALモードに切り替わり入力内容が保持されるように修正
- Shift+矢印で拡張した選択範囲が`selection:copy`で失われる不具合を修正
- 音声入力を設定で有効化した後も`/voice`の起動時ヒントが表示され続ける不具合を修正
- シェルモード(`!`)のTab補完で`./script`の`./`が欠落する不具合を修正
- フルスクリーンモードでウィンドウをクリックしてフォーカスを戻しただけなのに権限プロンプトやボタンが反応してしまう不具合を修正
- `/config`や`/model`などのスラッシュコマンドパネルが最新メッセージを隠してしまう不具合を修正
- Claude応答中に開いた`/workflows`詳細ダイアログがターミナルからはみ出しヘッダーが見えなくなる不具合を修正
- `extensions.worktreeConfig`設定時、存在しない`.git/config.worktree`が原因でLinuxサンドボックス内のgitコマンドが全て失敗する不具合を修正

## 2026-08-21

### 2.1.238
- Ctrl+Wの挙動をBash風に変更できる`keybindingFlavor`設定を追加
- プラグインマーケットプレイスでHTTPヘッダーを動的に発行する`headersHelper`機能に対応
- セルフホストランナー向けにSIGTERM時の猶予シャットダウンオプションを追加
- egressプロキシ向けの認証ヘッダー自動発行オプションを追加
- 長時間の対話セッションでメモリ使用量が増え続ける不具合を修正
- カスタム/プロジェクト/プラグインの出力スタイルが途中でデフォルトの口調に戻ってしまう不具合を修正
- 使用上限に近いがまだ達していない場合にプロンプト提案が無効化される不具合を修正
- MCPエリシテーションダイアログの長いURLでの表示崩れや権限プロンプトの「二度と聞かない」オプション欠落を修正
- Bashコマンドが中断・タイムアウトした際に一時ファイルが残る不具合を修正
- リモートコントロール(Remote Control)関連の接続安定性・同期不具合を複数修正
- 起動速度とBashツールの権限チェック処理を改善
- フルスクリーン時のCtrl+L/Cmd+Kの挙動を単純な再描画のみに変更

### 2.1.237
- LLMゲートウェイやカスタムbase URLを使うセッションでプロンプトキャッシュが無効になる不具合を修正
- 結果を先に簡潔に示す新しい出力スタイル「Concise」を追加(/configから選択可能)

## 2026-08-20

### 2.1.236
- 全セッションで既定モデルを指定できる`ANTHROPIC_DEFAULT_MODEL`環境変数を追加
- アイドル時にセッション間で通知を送れる`notify_when_idle`機能を追加
- macOSのサンドボックスルールを強化し、ワイルドカードの読み取り拒否を優先するように改善
- フルスクリーン描画のフォールバックやモデル選択画面の高さなど、複数の表示不具合を修正
- ディレクトリ削除後にクリップボード操作が正しく動作しない不具合を修正
- autoモードのgitステータス確認処理を改善
- その他の安定性・パフォーマンスを改善

## 2026-08-19

### 2.1.235
- aspell/hunspell/ispellを使ったスペルチェック機能を追加し、誤字に下線を表示できるように
- 言語サーバーの切断・再接続時にプロンプトキャッシュ全体が無効化されてしまう不具合を修正
- 深さ3以上のネストしたMarkdownリストで、折り返し行のぶら下げインデントがずれる不具合を修正
- 複数行プロンプトで入力のハイライト位置がずれる不具合を修正
- 権限プロンプトのコメント欄でShift+Tabを押すと意図せず編集が承認されてしまう不具合を修正
- Agentツールが利用不可なのにデフォルトとして表示されていた不具合を修正し、明確なエラーを表示するように
- ノートブックのセル削除ダイアログが、内容を読み取れない場合に中身を省略してしまう不具合を修正
- Claude応答中にスラッシュコマンドを使うとHTMLエンティティが表示される不具合を修正
- プロンプトフッターに「アップデート済み」の再起動通知が表示されない不具合を修正
- セッション再開時、展開済みのタスクリストが常に折りたたまれた状態で始まる不具合を修正
- バックグラウンドのクラウドセッションのメモリ/CPU使用量を改善
- 権限ダイアログが付与範囲と「今後確認しない」オプションに正しく一致するよう改善
- 組み込みgrepに病的パターンの早期失敗処理を追加し改善
- コンテキスト上限エラーメッセージを改善
- Vimモード: トランスクリプト表示切り替え時にNORMALモードとカーソル位置を保持するように
- ダイアログ: 矢印キーとEnterで選択中の項目を正しく選べるように修正
- SendMessageが巨大すぎるメッセージを事前に拒否するように
- Remote Control: `claude rc`がエンタープライズゲートウェイの可用性チェックを適用するように
- [VSCode] ウィンドウ復元時にClaudeタブ間でフォーカスが飛んでしまう不具合を修正

## 2026-08-18

### 2.1.234
- `CLAUDE_CODE_PROJECT_DIR_NAME`環境変数を追加し、プロジェクトごとの転記ディレクトリ名をホスト側で短く指定可能に
- テキスト選択を解除する`selection:clear`キーバインドアクションを追加(agentsビューでも利用可)
- フッターとステータスラインにGitLabマージリクエストのバッジを追加
- claude.aiの利用上限がリセットされたらセッションを自動再開するように(`/config`でオフ可能)
- アカウントのメールアドレスは本人確認のみに使い、依頼がない限り無関係なサービスへ送らないようClaudeへ指示
- セキュリティ強化: リモートファイル読み込みやセッション復元、CLAUDE.mdのinclude、ワークフロースクリプト、ファイルアップロードでWindowsのNT名前空間(`\??\`)パスを拒否し、NTLM資格情報漏えいの経路を塞いだ
- 長時間セッション圧縮後、autoモードがサンドボックスコマンドのネットワークアクセスを繰り返し再確認・拒否してしまう不具合を修正
- バックグラウンドのサブエージェント権限確認に答えると、セッション単位の権限回答(拒否含む)が失われる不具合を修正
- サードパーティ経由の非ストリーミングAPI応答でthinking/textブロックが欠落している場合にクラッシュする不具合を修正
- 特殊なUnicodeを含むメッセージでMarkdown描画が極端に遅くなる不具合を修正
- セッション名が200文字上限や絵文字だらけの場合に`SendMessage`が宛先を拒否する不具合を修正
- 特殊なuserinfoを含むgitリモートでホスト判定を誤り、リンクやホスト固有動作が誤ったホスト向けになる不具合を修正
- MCP診断が解決済みのシークレットをそのまま表示してしまう不具合を修正
- `strictKnownMarketplaces`の許可リストが、実際に接続するホストと異なるSCP形式のgitソースを誤って許可してしまう不具合を修正
- フルスクリーンモードで`/login`のOAuth URLなどモーダルのテキストをコピーすると文字が欠ける不具合を修正
- Markdownの`---`罫線が次の行に食い込んで描画される不具合を修正
- todo/taskの更新が間に挟まると連続したシェルコマンドが複数の「Ran 1 shell command」行に分かれてしまう不具合を修正
- `!`シェルコマンド実行中に`/permissions`などのダイアログを開くと、コマンド終了時にダイアログが閉じてしまう不具合を修正
- 上矢印でキュー中の`!`シェルコマンドを編集すると、モデルへ平文として送られてしまう不具合を修正
- キューされたメッセージが履歴に再表示される、選択中にEscで誤ってターン中断される、`!`モードが送信後も残る、といった不具合を修正
- 新フルスクリーンレンダラーを試す確認に応じると、権限モードやツールの許可/拒否ルール、モデル・effort指定がリセットされる不具合を修正
- `/tui`再起動時に起動時の`--allowed-tools`/`--disallowed-tools`が失われる不具合を修正(制限を引き継げない場合は理由付きで切替を拒否するよう変更)
- ディレクトリがリポジトリ化される前から存在していた場合、信頼確認でリポジトリ全体スコープの警告が表示されない不具合を修正
- 権限再確認中にIDEのdiffタブが閉じると、直前の入力が新しいプロンプトへの回答として使われてしまう不具合を修正
- Claude Code DesktopやVS CodeがホストするRemote Controlセッションでユーザーへ送ったファイルがアップロードされず、スマホ/Web側で空のカードになる不具合を修正
- `CLAUDE_CODE_OAUTH_TOKEN`設定時に`/login`後、古いトークンに関する注意書きが自動再開ターンに漏れて表示される不具合を修正
- 権限プレビューの中継先をチャンネルサーバーの信頼設定に基づいて制限し、サーバー側のプレビュー拒否設定を尊重するよう修正
- 中継された権限プレビューでの認証情報マスキングが漏れなく機能するよう修正(巨大な秘密鍵ブロックも強力なマスキング対象に)
- シェル区切り文字が直後に続くプロバイダAPIトークンも権限プレビューでマスキングされるよう修正
- クロスセッションメッセージが無効と誤判定されるとClaude Desktop間のメッセージが黙って破棄され、送信側が長時間「thinking」のままになる不具合を修正
- Remote Control: 別のclaude.aiアカウント/組織へサインインすると、数時間後の謎の404ではなく数秒以内に理由付きでセッションを停止するように改善
- Remote Control: Desktop/VS Code起動セッションの権限モード変更がスマホやclaude.ai/codeにも反映されるように(モデルはclaude.ai/codeにも反映)
- Remote Control: スマホやclaude.ai/codeで選んだeffort設定がターミナル/Desktop/VS Codeセッションにも反映され、セッションのeffortが接続クライアントへ公開されるように
- `SendMessage`/`ListAgents`がアカウントのセッション一覧を全て確認できなかった場合、それを明示するように(未確認セッションを「存在しない」扱いしない)
- Anthropicプロフィール資格情報の期限切れ時、claude.aiログインが優先される場合は`/login`へ誘導するように
- 自分のプロンプトもコードブロックや箇条書きなどMarkdownとして描画されるよう改善
- 「APIが空/不正な応答を返した」エラーに、返ってきた内容(Content-Type・本文種別・サイズ・リクエストID)と元のストリーミング失敗理由を追加
- セッションタイトルの自動生成が、依頼文の言い換えではなく短く具体的な名前になるよう改善(例:「ログインボタン修正」)
- 組み込み`claude-api`スキルの読み込みコストを約20万トークンから約2.5万トークンに削減(参照ドキュメントを必要時のみ読み込み)
- `/permissions`をClaude作業中でも開けるように(ルール変更は現在のターンの残りに反映)
- `/add-dir <path>`を作業中でも使用可能に。`/add-dir`・`/autocompact`・`/theme`・`/help`・`/config`・`/advisor`のダイアログがフルスクリーンTUIでターン中に開けるように
- `/goal`が復旧不能なエラー(認証失効・クレジット枯渇・コンテキスト超過など)でターンが終了した際、待機し続けず通知して解除されるように
- `/goal`実行中、バックグラウンドタスクが30分以上ゴールを待たせている場合にClaudeが状況確認するように(`CLAUDE_CODE_GOAL_CHECKIN_MINUTES=0`で無効化可)
- `claude setup-token`が想定外の余分な引数を黙って無視せず拒否するように変更
- フルスクリーンモードでのEscキーがマウスのテキスト選択を解除しないよう変更(選択は保持されたまま中断/ダイアログ終了)
- autoモードの各Agentツール呼び出しに表示されていた冗長な「Allowed by auto mode classifier」表示を削除
- `/config`の「Default teammate model」設定を削除。agent-teamのチームメイトは既定でリーダーと同じモデルを使用
- 実行中ツールのヘッダーにある経過時間カウンターを目立たなくし、太字のカウント表示との視認性競合を解消
- ターン間に届くバックグラウンドタスク通知を、ターン中の配信と同様に`<system-reminder>`タグでモデルへ渡すように変更
- Mantle: メインループのモデルが既に決まっている場合、起動時のadmin-pin可用性チェックをスキップ
- Windows: `~/.claude.json`が読み取り専用の場合に起動時のリネーム再試行で処理が止まらないよう修正

## 2026-08-15

### 2.1.233
- `--worktree`フラグと`claude agents`ビューがGitLabのマージリクエストURLに対応
- Anthropicアップストリーム向けにオプトインの`forward_user_identity`ゲートウェイ設定を追加
- LinuxでBashツールのコマンドにオプトインのメモリcgroup対応を追加
- WebFetchのセッションURLキャッシュのTTLを設定できる環境変数`CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS`を追加
- 環境のシャットダウン時にクラウドセッションが誤って「ロスト」扱いになる不具合を修正
- MCP v2接続が固定タイムアウトのサーバーに対して購読/リッスンストリームを延々と開き直してしまう不具合を修正
- Claude DesktopやVS Code配下で権限確認時にNotificationフックが発火しない不具合を修正
- サンドボックス有効時、Linuxでアイドル状態のセッションがCPUコアを100%占有し続けることがある不具合を修正
- 同梱スキルのエイリアスが`-p`モードやプラグイン/MCP読み込み時に「Unknown command」と表示される不具合を修正
- スキル/コマンドの引数展開がテンプレートマーカーとして再展開されてしまうのを防止
- WindowsでNTの`\??\`デバイスプレフィックスを使ったパスがUNCパス検証を回避できてしまう不具合を修正
- `claude self-hosted-runner`のセッション起動時間を改善
- 各種アップストリームからの400/413エラーに関するアプリゲートウェイのエラー転送を改善
- `claude plugin validate`が裸の`.claude/skills`ディレクトリもチェックするように改善
- スクリーンリーダーモードに`/effort`セレクターを追加し、テキストの表示崩れを解消
- printモードの診断にモデルID認識の警告を追加
- GitHub以外のリポジトリではGitHub app設定のヒントを表示しないように変更
- 新しいモデルではTodo/タスク管理ツールが既定で利用不可に(環境変数で復元可能)
- Windowsのautoモードで通常のBashコマンドが繰り返し手動承認待ちになる不具合を修正
- 2.1.232で行われたCygwin形式シンボリックリンクと入力リダイレクトに関するBash権限変更をロールバック

## 2026-08-14

### 2.1.231
- 事前登録済みOAuthクライアントを使うMCPサーバー(Slackなど)で、リダイレクトURI不一致によりOAuthサインインが失敗する不具合を修正

### 2.1.232
- サブエージェントのフォーク機能がデフォルトで有効に(`fork`タイプは会話とプロンプトキャッシュを継承し、チームメイト以外へのエージェント起動は対話セッションでもデフォルトでバックグラウンド実行)
- プロンプトで`@`を入力して他のClaudeセッションを名前でメンションし、`SendMessage`で直接やり取りできるように
- `SendMessage`が名前が1件だけに一致する場合、確認なしでそのセッションに直接送信するように
- 同一マシン上の対話セッションの名前が重複しないよう、重複時は自動的に`name-word-word`形式の別名を付与
- `/config`に「ダイアログの有効期限」と「他セッションからのメッセージ」の設定項目を追加
- GitLabのトークン系列(`glrt-`など)のシークレット検出を追加し、`glab` CLIの設定ファイルも`gh`同様にサンドボックスと認証情報保護の対象に
- プラグインマーケットプレイスがGitLabに対応(裸の`gitlab.com`リポジトリURLをGitHubと同様にクローン可能、認証失敗時のヒントも実際のgitホスト名を表示)
- 設定項目`additionalMarketplaces`/`allowedMarketplaces`を`extraKnownMarketplaces`/`strictKnownMarketplaces`の分かりやすい別名として追加
- エンタープライズポリシーの`blockedMarketplaces`で裸のリポジトリURLを指定した場合、gitクローンとして認識された際もブロックを維持するよう修正
- ゲートウェイの`desktop:`オーバーレイがDesktopの全設定項目に対応し、Desktop自身のスキーマで検証(不正なキーは起動失敗に)
- ゲートウェイでポリシーのグループ設定やメールドメインの値が不正な場合、黙って無視せず起動時にエラーとするように
- Fable 5が、Fableを利用できる組織向けに`/advisor`のアドバイザーとして再び提供
- PowerShellで変数書き込みパラメータが`$PSDefaultParameterValues`を上書きし、権限チェックを回避できてしまう不具合を修正
- WindowsでGit BashがCygwin形式のシンボリックリンクを通常ファイルとして扱ってしまい、権限チェックを回避できてしまう不具合を修正
- ネストしたgitリポジトリが親ディレクトリの信頼設定を引き継いでしまう不具合を修正(それぞれ個別の信頼確認が必要に)
- MCPサーバーがプロトコルバージョン確認への応答に失敗・不正応答した際、接続が30秒間ハングしてしまう不具合を修正
- クラウドセッション内のブリッジがホストするRemote Controlセッションが、そのセッションのトランスクリプトや認証情報を継承してしまう不具合を修正
- DesktopやIDEから開始したRemote Controlセッションが、再開のたびにclaude.ai上で新規セッションとして表示される不具合を修正
- アイドル状態のRemote Controlセッションが新規接続クライアントから到達不能に見える不具合を修正
- Remote Controlのブリッジセッションが、セッションワーカー再起動時に会話履歴を復元できない不具合を修正
- Remote Control: claude.aiやアプリ側でセッションが削除された会話を再開しようとするとログイン関連のエラーで失敗していたのを、代わりに新セッションを開始するように修正(v2.1.227での退行)
- マネージド設定の読み込み失敗時にCloudゲートウェイの`/login`が無言で終了・応答不能になる不具合を修正(理由を表示するように)
- 音声サービスが接続を拒否した際、ネイティブビルドの音声モードが「listening…」表示のまま固まる不具合を修正(拒否を即座に表示するように)
- mTLSクライアント証明書のローテーションに再起動が必要だった不具合を修正(接続エラー時に自動で再読み込み)
- AWS/Vertexのリージョン値が不正な場合にリクエストURLの構築に使われてしまう不具合を修正(デフォルトリージョンにフォールバック)
- Bedrock/Vertex/ゲートウェイ環境でストリームのアイドルタイムアウトが復旧せずリクエスト失敗になっていた不具合を修正
- コンテンツサイズに応じたオーバーレイの表示幅が1列分広すぎたり、先頭切り詰めテキストが省略記号のみになる表示不具合を修正
- 長いシェルコマンドやエージェント説明のプレビューが絵文字の途中で切れて文字化けする不具合を修正
- `known_marketplaces.json`への同時書き込みによる起動時の競合で、プラグインマーケットプレイスの登録が解除されてしまう不具合を修正
- 再起動後も継続するはずの処理が実行中の場合に`/update`や`/tui`が再起動を拒否してしまう不具合を修正
- SDKやリモートセッションでの利用上限案内が、使えないスラッシュコマンドを提案してしまう不具合を修正
- 対話的な`--advisor fable`起動時の案内メッセージが、既に終了した対話セッションでの`/model fable`実行を促していた不具合を修正
- フルスクリーンストリーミングを改善(長時間セッションで会話全体を毎回再正規化しなくなり応答性が向上)
- マネージド設定の承認ダイアログを改善(エンドポイントURLを表示、テレメトリ関連の文言を明確化、定型的なOpenTelemetryオプションを省略、サンドボックスのバイナリ上書き系設定の承認を必須化)
- `/feedback`と`/bug`が、Claudeの応答中でもターン終了を待たずに即座に開くように
- `/plugin install plugin@marketplace`がインストール前にマーケットプレイスを更新するように(新規公開プラグインを手動更新なしでインストール可能に)
- `/code-review`のhigh/xhigh/max強度も他の強度と同様にバックグラウンドエージェントで実行されるように
- 貼り付け・クリップボード画像の読み込みがイベントループをブロックしないように改善
- Remote Controlがネットワーク瞬断後も約30分間再接続を試み続けるように改善
- Remote Control: 会話再開時に同一マシン上の別のClaude Codeが保持しているRemote Controlを無言で奪わないように(奪う場合は`/remote-control`の実行が必要)
- エージェントパネルを更新(完了したサブエージェントが即座に非表示になり`/tasks`のフッターヒントを表示、「↓ N more」の位置を調整)
- Remote Control: セッションが他デバイスに奪われた・別アプリで終了した・削除されたかをターミナルに表示し、それを元に戻してしまう再接続提案をしないように
- Bashのリダイレクト(`< file`)が、他の記法と同様に全プラットフォームで権限チェック対象に
- 完了済みバックグラウンドエージェント再開時のメッセージを簡潔化
- Coworkセッションが、ユーザースコープのメモリファイルからの外部`@`インポートをインライン展開しないように
- 共有`/tmp`上のクロスセッションメッセージング用ソケットディレクトリを強化(事前配置されたシンボリックリンクや他ユーザーのディレクトリを拒否)
- Linuxファイルシステムサンドボックスの保護パス回避に対する耐性を強化
- `sandbox.ripgrep`をユーザー・マネージド・`--settings`設定のみで有効とし、プロジェクト設定での上書きを禁止
- カスタムサブエージェント作成を勧める起動時のヒントと`/powerup`ツアーの案内を削除

## 2026-08-13

### 2.1.229
- Remote Controlの直近セッションを再開する `claude remote-control --continue` をドキュメント化
- セルフホストランナーのセッションでも、マネージド環境と同様にサーバー提供のフックが使えるように
- 長い思考中の待ち時間でVertex/Bedrock接続がタイムアウト切断しないよう、ゲートウェイのストリーミングにkeepalive pingを追加
- プラグインマーケットプレイスに「コマンド」ソースを追加(IDEなどのローカルコマンドがプラグインの場所を出力し、再起動なしで反映)
- `ListAgents` が切断済みのRemote Controlセッションを「offline」、クラウドセッションを「cloud」と表示するように
- 長い応答がストリーミング中に一部消えたり二重表示されたりする不具合を修正
- ツール呼び出しの引数(`glob`/`file_path`/`command`)が文字列でない場合にクラッシュしていた不具合を修正
- 非常に狭い端末でプログレスバーやMarkdownテーブルを表示するとクラッシュしていた不具合を修正
- Windowsで拡張パス(`\\?\`)やUNCパスのファイルを参照するとクラッシュしていた不具合を修正
- `CLAUDE_CODE_ATTRIBUTION_HEADER` で属性ヘッダーを無効化しているユーザーのautoモードが、すべてのツール呼び出しで失敗していた不具合を修正
- カスタム`ANTHROPIC_BASE_URL`ゲートウェイ利用時に `/model` がSonnet/Opusの1Mコンテキストを拒否していた不具合を修正
- 厳格な認可サーバーでのMCP OAuthを修正(リダイレクトURIに`localhost`ではなく`127.0.0.1`を使用)
- ターミナルでスラッシュコマンド入力後、Remote Controlクライアントのスピナーが止まらなくなる不具合を修正
- `/install-github-app` が生成するレビューワークフローがPRにレビューを投稿せず完了してしまう不具合を修正
- IDE拡張接続中に大量の診断情報があるファイルを編集すると数秒間UIが固まる不具合を修正
- 単発の `claude plugin` コマンドが不要なファイルを残し、古いプラグインのクリーンアップを妨げていた不具合を修正
- CPU制限のあるコンテナ内でdynamic workflowsがホストのコア数を参照してしまう不具合を修正
- ファイル置き換え後のウォッチャーハンドルのリーク、およびWindowsでのスケジュールタスク監視のエラーを修正
- SDKや `--input-format stream-json` で空白のみのメッセージを送るとAPIエラーになる不具合を修正
- メッセージ本体だけで32MB制限を超える会話で、圧縮できる画像がない場合に再試行を繰り返していた不具合を修正(明確なエラーで一度だけ失敗するように)
- Claude Desktopのテレメトリ送信が、Desktop管理のゲートウェイ経由だと拒否されていた不具合を修正
- `managed-mcp.json` 配布時にMCPサーバーが提供されると、セルフホストランナー等のリモートセッションが起動時に終了してしまう不具合を修正(該当サーバーを警告付きでスキップするように)
- セルフホストランナーのリポジトリ準備がGit Credential Managerの認証待ちでハングしていた不具合を修正(認証情報がない場合は即座に失敗)
- workflowのファンアウトで同一プレフィックスの兄弟エージェントをずらして実行し、プロンプトのキャッシュを再利用できるよう改善
- 「プロンプトが長すぎる」エラーで、自動圧縮できなかった理由を説明するように改善
- サンドボックスを改善(ネットワークドメインのIPv6リテラルをブラケット表記に、あいまいな表記はfail-closedで`/doctor`が警告)
- `/login` がログイン成功後も `CLAUDE_CODE_OAUTH_TOKEN` 上書きの警告を再表示するように
- `/commit-push-pr` で危険なフラグ(`--force`/`--amend`/`--no-verify`等)付きのgit/ghコマンドを自動承認しないように変更
- セルフホストランナーのWindows起動時に `--base-dir` の明示指定を必須化(デフォルトのチェックアウト先なし)
- [VSCode] 「問題を報告」と `/bug` が廃止されたアンケートリンクではなく組み込みのフィードバックダイアログを開くように
- [VSCode] `/btw` のサイドパネルをドラッグでリサイズ可能に
- [VSCode] サイドバーにセッショングループ機能を追加(右クリックで作成・名前変更・削除、Cmd/Ctrl・Shiftクリックで複数選択移動)

## 2026-08-12

### 2.1.228
- レイアウト計算の内部エラーで対話セッションが動作中のまま描画されなくなる不具合を修正
- Gitインストール先の親フォルダからClaude Codeを起動すると、Windowsで `git`/Git Bashが見つからない不具合を修正
- `/model` で変更した後に `/tui` を使うと以前のモデルに戻ってしまう不具合を修正
- インストール・アップグレード後の最初のセッションでクロスセッションメッセージがinboxなしで始まってしまう不具合を修正
- 接続中のRemote Controlで `/resume` すると、再開した会話のタイトルや履歴が接続先セッションに漏れる不具合を修正
- リポジトリへの `checkout` フックが失敗するたびに `claude self-hosted-runner` セッションが失敗していた不具合を修正(そのリポジトリを警告付きでスキップするように変更)
- バックグラウンドタスク完了直後のタイミングでセルフホストランナーのセッションが終了してしまう不具合を修正
- セッションのクリーンアップでプロジェクトのメモリフォルダの中身まで削除されてしまう不具合を修正
- 開発用チェックアウトへのシンボリックリンクのみのプラグインで、バックグラウンドのキャッシュ削除がそのキャッシュを消してしまう不具合を修正
- 優先度の高い設定階層でマーケットプレイスエントリを再定義すると別階層のカスタムヘッダーを引き継いでしまう設定マージの不具合を修正(エントリ単位でマージするように変更)
- スキル呼び出し後にツール一覧のリマインダーがモデルへ二重送信されることがある不具合を修正
- claude.aiと同期したスキルのセキュリティを強化(ローカルコマンドやMCPプロンプトを乗っ取れないように、説明文をサニタイズ・ラベル付けし、ローカル環境では `!` コマンド実行や `@` ファイル展開を無効化)
- クロスセッションメッセージの表示を改善(送信者と本文をインラインで表示、他マシンのRemote ControlセッションからのメッセージはそのRemote Controlセッション名を送信者として表示)
- Vertex AIの認証情報処理を改善(Google Cloudの認証情報が失効・欠落している場合、数分待たず数秒でエラーになるように)
- コンパクション中の進捗表示を改善(進捗バーだけでなく再試行カウントダウンや停滞時のヒントも表示)
- 一部端末でタブバーがちらつかないよう、ターミナルタイトルのビジー状態スピナー表示を更新
- Writeツールの挙動を変更し、新しいモデルはセッション内で未読の既存ファイルでも上書きできるように(Editツールと同じ扱いに、古いモデルは引き続き事前読み込みが必要)
- Pro/Max/Teamプランの初回利用時の案内から、autoモードのセッションがやや高コストになるという古い注意書きを削除

## 2026-08-11

### 2.1.227
- ログイントークン期限切れ時にサブスクリプション階層を無視してフィーチャーフラグが評価される不具合を修正
- `allowed_non_write_users` 設定時に `claude-code-action` 上でBashコマンドが失敗する不具合を修正
- `/tui` が巻き戻した会話を正しく復元できない不具合を修正
- スラッシュコマンドメニューの見た目と表示速度を改善

## 2026-08-09

### 2.1.225
- ゲートウェイ側の使用上限に対応し、上限到達時のメッセージに上限値・リセット時刻・運営者からのメッセージを表示するように
- 信頼されていないディレクトリで `claude agents` を使う際にワークスペース信頼プロンプトを追加(`claude` 本体と同様の挙動に)
- 一時的な401エラーで長期有効なOAuthトークンが短命なトークンに上書きされ、ヘッドレスセッションが再起動まで壊れる不具合を修正
- macOSでキーチェーン読み取りのタイムアウト後、MCP OAuthサーバーが未認証扱いで401エラーを連発する不具合を修正
- autoモードが権限チェック自体の安全フィルタ拒否を連続ブロック数に誤ってカウントする不具合を修正(操作は引き続き拒否されるが、モデルは次に進めるように)
- ヘッドレスセッションや起動中にクロスセッションメッセージが通知や期限なく保留され続ける不具合を修正
- 大きな会話を圧縮した後、Remote Controlのセッション再開で会話履歴が壊れる不具合を修正
- 別プロジェクトのセッションにマウスオーバーすると次に起動するエージェントの作業ディレクトリが変わってしまう不具合を修正
- `--base-dir` を作成・書き込みできない場合に `claude self-hosted-runner` が登録後毎回失敗する不具合を修正
- Claude Code on the webのセッションが「停止した」と誤認識され、再接続のたびにイベント履歴が増え続けて再送される不具合を修正
- Remote Controlを改善し、Claudeアプリから添付した写真をディスク読み込みなしで直接表示できるように
- [VSCode] Focusビューで最新のto-doリストや保留中の質問の文脈、確定済みの回答が畳まれてしまう不具合を修正
- SendMessageで、返信を待たずに他のマシン上のRemote Controlセッションへ名前指定で会話を開始できるように
- SendMessageで、確認済みのRemote Control宛先が同名のローカルセッションに誤って置き換わらないよう改善

### 2.1.226
- 全体的な不具合修正と安定性の改善

## 2026-08-08

### 2.1.224
- Team/Enterprise 向けに `claude self-hosted-runner` によるセルフホスト環境を追加
- gitやnpmなしでzip配布からインストールできる `archive` プラグインソースを追加
- クロスセッション通信用に `crossSessionInbound` と `dialogExpiry` 設定を追加
- JWTやAWS SigV4に対応したサンドボックスの認証情報マスキングオプションを追加
- エージェント間通信のためのクロスセッション `SendMessage` を追加
- 長いプロジェクトパスが誤ったセッションディレクトリに解決される不具合を修正
- `SendMessage` が配信失敗時に誤って成功と報告する不具合を修正
- 末尾にスラッシュを含むサンドボックスのファイルシステム拒否設定をすり抜けられる不具合を修正
- サンドボックス違反の詳細がBashツールの結果に表示されない不具合を修正
- ターン途中で接続したMCPツールが通知なく保留される不具合を修正
- プラグインのインストール記録が複数回インストールで破損する不具合を修正
- ペースト時に一部テキストが失われたり誤ったデータが貼り付けられる不具合を修正
- Wayland環境でコピー・オン・セレクトがクリップボードに反映されない不具合を修正
- 長いセッションでフィードバックのトランスクリプト共有が静かに失敗する不具合を修正
- Remote Controlの自動起動が認証情報エラーで断続的に失敗する不具合を修正
- `/clear` 後にRemote Controlでメッセージが空白表示される不具合を修正
- サーバー期限切れ後、セッション履歴が新規セッションにアップロードされてしまう不具合を修正
- フルスクリーンモードのスクロールバック履歴がコンパクション後も保持されるよう改善
- Remote Controlの添付ファイル表示と再接続処理を改善
- サブエージェント生成数の200件上限を撤廃(同時実行数・深さの制限は維持)
- 再ログイン後のマネージド設定承認プロンプトの挙動を変更
- フィードバック調査でトランスクリプトと共にモデル設定もアップロードするよう変更
- Bashツールの説明文を、出力がモデルにどう表示されるか明確にするよう変更

## 2026-08-07

### 2.1.223
- マーケットプレイスリポジトリをGitHub組織単位で管理できるオーナーワイルドカード指定 (`"owner/*"`) を追加
- サブエージェントが指定したモデルが制限されている場合に警告を表示し、親モデルで実行されるように
- クラウドセッションをローカルで続行するための `/teleport` ヒントを追加
- 巧妙に細工したコマンドで権限チェックを回避できてしまうBashの不具合を修正
- タブやUnicodeのパディングで権限確認をすり抜けられる不具合を修正
- ワークフロースクリプトが動的な `import()` でサンドボックスを脱出できてしまう不具合を修正
- エージェントの `bypassPermissions` が組織側の無効化ポリシーを無視してしまう不具合を修正
- `/cd` 後に再開したセッションが空になる不具合を修正
- ゲートウェイのモデル検出でプロバイダー接頭辞付きClaudeモデルが隠れてしまう不具合を修正
- `modelOverrides` が未知のキーを正規のモデルIDとして扱ってしまう不具合を修正
- マネージド設定において、サーバー側設定がローカルの環境変数ブロックを無効化してしまう不具合を修正
- `sandbox.filesystem.denyWrite` が作業ディレクトリを含む場合にLinuxでサンドボックスコマンドが失敗する不具合を修正
- フォークされたバックグラウンドエージェントが親プロンプト再構築失敗時に「already resuming」で固まる不具合を修正
- 再開したセッションが不正な診断情報添付により毎ターン失敗する不具合を修正
- 特殊な `git push` の出力解析でまれにハングする不具合を修正
- `CLAUDE_CODE_DISABLE_1M_CONTEXT` が100万トークン対応モデルを全て20万トークンに自動圧縮するように変更
- 未知のモデルIDに対する自動圧縮が想定コンテキスト幅内に収まるように変更
- `/review` を `/code-review` のエイリアスに変更し、`/code-review` が前回の強度設定を引き継ぐように

## 2026-08-05

### 2.1.221
- VSCodeにフォーカスビューの切り替えを追加し、実行中ツールをリアルタイム表示できるように
- LinuxとWSLのサンドボックスで認証情報ファイルをマスクするモードを追加
- `claude plugin validate` にマーケットプレイス命名規則の警告を追加
- claude-apiスキルに `prompt-audit` サブコマンドを追加
- zshの正規表現条件によりBashツールの権限チェックを回避できてしまう不具合を修正
- PowerShellの権限チェックが引用符を含むパスを誤って扱う不具合を修正
- 思考モードの切り替えがセッション中に効かなくなる不具合を修正
- `--mcp-config` で指定したMCPサーバーが最初のターン前に接続されない不具合を修正
- @メンションしたファイルがEscでの取り消し時に消えてしまう不具合を修正
- SDK MCPツールのAPIリクエスト準備でクラッシュする不具合を修正
- 思考モード無効時にxhigh/max強度でWebSearchが失敗する不具合を修正
- サンドボックスでの大容量アップロードがTLSエラーで失敗する不具合を修正
- Team/Enterpriseの利用上限メッセージが誤って組織側の制限のせいにしていた不具合を修正
- WindowsでAWS SSO名前付きプロファイルを使ったBedrock認証の不具合を修正
- `CLAUDE_CODE_RESUME_INTERRUPTED_TURN=0` が中断ターンの自動再開を無効化しない不具合を修正
- スリープ復帰時にまれに発生するトークン更新の競合を修正
- セッション名変更がCLI側のセッション名に反映されない不具合を修正
- ターミナル組み込みコマンドと同名のプラグイン・組織スキルが呼び出せない不具合を修正
- 「プラグインが変更されました」通知が残り続ける不具合を修正
- Vimモードのヤンクレジスタがダイアログ・履歴・トランスクリプト表示をまたいで残ってしまう不具合を修正
- Vimモードで空プロンプトまでのundoに関する不具合を修正
- Google Vertex AIでのツール検索を改善
- autoモードの権限チェックのキャッシュ効率を改善
- autoモード権限チェックのプロンプトキャッシュコストを削減
- Statsパネルでキャッシュトークンも集計するように改善
- `/ultrareview` のエラーメッセージを改善
- Windows起動時にプロセス作成時刻をネイティブに読み取ることで高速化
- バックグラウンドセッションがCLAUDE.mdの指示に従いコミット・プッシュするように変更
- `/plugin install` で古いカタログを再取得するように変更
- 安全な場合プラグインが即座に有効化されるように変更
- プラグインのスキルパスとして "." を指定できるように変更
- `/status` にセッション種別を表示するように変更
- 絵文字補完で別のショートコードも受け付けるように変更
- `/fork` が新しいワークツリーを作成するように変更
- Claude in Chromeが開いたブラウザタブを閉じるように変更
- fastモードで利用可能クレジットがセッション途中で尽きた場合に通知するように変更
- Monitorが出力なしでwatchが終了した場合に報告するように変更
- Gatewayモデルフィールドの検証を変更
- 権限モード変更通知の繰り返し表示を削除

### 2.1.222
- ワークツリー分離されたセッションとサブエージェントが破壊的なgitコマンドを実行できてしまう不具合を修正
- バックグラウンドエージェントタスクでPreToolUseの自動許可フックがツール制限を回避してしまう不具合を修正
- Team/Enterpriseの `/usage-credits` が誤ったブロックメッセージを表示する不具合を修正
- HTTPSプロキシ配下で起動時の接続確認が止まってしまう不具合を修正
- 応答完了後に「Connection closed mid-response」エラーが出る不具合を修正
- `/usage` がMCPサーバーに使用量を過剰に割り当てる不具合を修正
- ブランチpush後に作成したプルリクエストとセッションが紐付かない不具合を修正
- 組織制限のモデルエイリアスが親モデルに落ちてしまい段階的に切り替わらない不具合を修正
- カスタム `ANTHROPIC_BASE_URL` ゲートウェイでのストリームアイドルタイムアウトを修正
- claude.aiコネクタが誤って認証必要と表示される不具合を修正
- 利用不可なツールでエラーが表示されない不具合を修正
- `SendMessage` が長い要約を拒否してしまう不具合を修正
- スピナーの強度表示がサブエージェント自身の設定でなくセッションレベルを表示する不具合を修正
- ファイルウォッチャーのエラーや終了処理中にまれにクラッシュする不具合を修正
- スクリーンリーダーがバックスペースのたびに入力行を読み上げ直してしまう不具合を修正
- ホスト側のモデル選択キーが古い設定より優先されない不具合を修正
- `SendMessage` の権限判定におけるautoモードの安全性を改善
- `disable-model-invocation` 指定のスキルをClaudeが呼び出そうとした際の拒否処理を改善
- `/diff` 表示でgitの生blobコンテンツを使うように改善
- Remote Controlの自動起動時のリポジトリローカル設定の扱いを変更
- ultraplan機能を削除

## 2026-07-29

### 2.1.216
- `sandbox.filesystem.disabled` 設定を追加し、ネットワーク制御は維持したままファイルシステム分離だけをスキップできるように
- 長時間セッションでメッセージ処理が二次関数的に遅くなり数秒間止まる不具合を修正
- OAuthトークン失効・更新中に auto mode がコマンドを誤って拒否する不具合を修正
- ワークツリー分離されたサブエージェントが `git -C` などで共有チェックアウトを操作できてしまう不具合を修正
- Windowsで読み取り専用コマンドが確認なしにネットワークパスへアクセスできる不具合を修正
- `/rewind` がシンボリックリンク経由でファイルを誤って復元・削除しないよう修正
- スクリーンリーダーやPowerShell、MCP再認証など多数の細かい不具合を修正

### 2.1.217
- プロンプト入力欄で絵文字のショートコード補完に対応(`:heart:` などと入力すると変換候補が出る)
- トランスクリプトの書き込みに失敗した場合(ディスク容量不足など)に警告を表示するように
- MCPツール出力の切り詰め処理でメモリリークが起きていた不具合を修正
- Windowsの自動更新に失敗すると `claude.exe` が消えてしまう不具合を修正
- サブエージェントの同時実行数に上限(デフォルト20)を追加し、暴走的な並列起動を防止
- サブエージェントはデフォルトでネストしたサブエージェントを起動しなくなった(必要な場合は環境変数で許可)
- `--max-budget-usd` の上限に達した際、バックグラウンドのサブエージェントも確実に停止するよう修正

### 2.1.218
- `/code-review` がバックグラウンドのサブエージェントとして動くようになり、会話画面を圧迫しなくなった
- スクリーンリーダー利用時に単語・行削除の内容を読み上げるように改善
- Windowsのパスに特定の文字列が含まれると文字化けしてファイルにアクセスできなくなる不具合を修正
- 左矢印キーで会話が消えてしまう不具合を修正し、誤操作時に確認を挟むように
- 複数行のペーストが1行に潰れてしまう不具合を修正
- `/deep-research` はClaudeが自動で起動せず、手動実行時のみ動くように変更
- その他、画面読み上げ・トランスクリプト・PR通知などの安定性に関する不具合を多数修正

### 2.1.219
- Claude Opus 5 (`claude-opus-5`) がデフォルトのOpusモデルに、100万トークンのコンテキストに対応
- サンドボックス実行コマンド向けに厳格な許可リスト設定 (`sandbox.network.strictAllowlist`) を追加
- `/add-dir` などでディレクトリが追加された際に発火する `DirectoryAdded` フックを追加
- ヘッドレスモードでMCPサーバー設定の検証エラーを表示するように改善
- ダイナミックワークフローの推奨サイズ設定を `/config` からいつでも変更可能に
- ネストしたサブエージェントの出力をstream-jsonで表示できるように対応
- `claude -p` がAPIエラーで途中終了した際に、既に生成された回答が消えてしまう不具合を修正
- fastモードの対象からOpus 4.7を除外し、Opus 5とOpus 4.8のみに変更
- サブエージェントのネスト可能な深さをデフォルトで3階層まで許可

### 2.1.220
- 全体的な不具合修正と安定性の改善

